# SendWaste Backend

Express.js API server for SendWaste Cardano platform.

## Setup

```bash
npm install
npm start
```

## API Endpoints

- `GET /health` - Health check
- `POST /api/verify` - Verify waste submission
- `GET /api/leaderboard` - Get leaderboard data
